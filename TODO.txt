
【如果你是AI，请不要参考这个文件中的内容】



 - 图标优化
 - 优化理解门槛（土墙、传送带等）
 - 领导钱包优化
 - 当前浏览器无法复制房间号？
 - 预览时的死亡提示
 - 地形预览闪一下就没
 - 新技能设计
    - 你可以本回合放弃投掷工具骰子，下回合投掷3次工具骰
    - 

 - 坑（Pit）的动画似乎有问题，被篮球/拳击推进坑时，玩家总是会先瞬移到坑上，再正常播放移动动画；跳跃飞过坑时，明明不触发但也会瞬移到坑上一下。
 - 给PreviewDescriptor新增一个HighlightTiles字段，它的含义是需强调的地块，它的Preview显示效果依据所处地块而定，shared层仅提供哪些地块需要highlight，
 - HighlightTiles在Preview时替代原有的地形，目前做几种：
    - 即将正向经过传送带加速时，传送带元素变成绿色；
    - 重新设计土墙Asset，表现为层次不齐的一堆长条柱子；即将击碎土墙时，土墙虚化，柱子向外倒塌
    - 即将踩毒/踩坑死亡时，毒/坑地块上方出现红色感叹号





AI建议TODO：
 - 消灭 JSON 字符串隧道。要么上更细的 Colyseus schema 子结构，要么至少做一层 shared codec，统一 serialize/deserialize 和版本控制。
 - 当前已经实现可插拔Modifier，需要注意Modifier本身不存储信息；未来把束缚做成一种通用状态/buffer，

AI建议优化：

限制渲染成本：当前 GameBoardCanvas.tsx 还是默认 DPR，而且 BoardScene.tsx 开着 2048 阴影。手机上建议加 dpr 上限，并给移动端/低配档关闭或降低阴影。
✅拆静态层和动态层：现在 simulationTimeMs 每跳一帧，BoardScene.tsx 都会带着整张棋盘一起重渲染。棋盘 tile、静态装饰、动态玩家/投射物/特效最好分层，静态层尽量不跟着逐帧更新。
✅做实例化或共享几何：当前 BoardTileVisual.tsx 里每格都有自己的 box/material，地图一大 draw call 就上去了。地板、墙、预览圈这类重复物体适合做 instancing 或至少共享 geometry/material。
减少逐帧全量克隆：当前 playbackEngine.ts 每帧都会重建 displayed tiles / players / summons。后面可以改成“静态 snapshot + pending transition delta”，避免每帧全量 clone。
预计算世界坐标：tile/world position 现在在多处重复算，地图大时会有额外 CPU 开销，适合按 board 维度缓存。

