# 房间与大厅流程

## 房间阶段

`roomPhase` 当前有三个阶段：

- `lobby`
- `in_game`
- `settlement`

## 大厅阶段

大厅阶段由 `packages/server/src/rooms/WatcherRoom.ts` 负责：

- 玩家加入房间
- 设置昵称与宠物
- 选择角色
- 切换 ready
- 房主开始游戏
- 房主踢人
- 断线重连

此阶段不走 shared turn orchestration。

## 开局

开局时 room 会：

1. 重置运行时状态与棋盘可变状态
2. 按地图出生点重置玩家位置
3. 清空事件日志与表现状态
4. 将 `roomPhase` 设为 `in_game`
5. 调用 shared `advanceTurn()` 开启首回合

注意：从这一步开始，回合推进由 `packages/shared/src/gameOrchestration.ts` 接管。

## 对局阶段

客户端只发送高层命令：

- `rollDice`
- `useTurnStartAction`
- `useTool`
- `endTurn`
- `setCharacter`
- `grantDebugTool`

`WatcherRoom` 只做：

- command 收包
- schema -> shared 映射
- 调用 `dispatch(command)`
- shared -> schema 回写

如果 shared 返回待推进状态，room 只按 `latestPresentation.durationMs` 挂 timer，之后调用 `advanceTurn()`。

## 结算阶段

当前结算主要用于竞速模式：

- 所有玩家完赛后，shared 将快照推进到 `settlement`
- client 根据 `settlementState === "complete"` 显示结算覆盖层
- 玩家可通过 `returnToRoom` 回到原房间大厅

## 回房

`returnToRoom` 会：

- 把 `roomPhase` 设回 `lobby`
- 重置 match runtime
- 保留房间与玩家座位
- 清空 ready 状态

## 对 AI 的约束

- 大厅与房间生命周期改动，可以在 `WatcherRoom.ts` 内处理。
- 一旦涉及回合推进、换人、终点推进、回合开始停留结算，必须回到 `packages/shared/src/gameOrchestration.ts`。
- 不要在房间层补“临时 turn 特判”。
